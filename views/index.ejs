<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
     <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link rel="stylesheet" href="/src/output.css" />
    <title>Where the ISS at? (Pete)</title>
  </head>
  <body class="bg-red-300 h-screen flex items-center justify-center px-4 md:justify-start">
    <div
      class="container px-3 py-16 bg-amber-50 max-w-7xl flex flex-col gap-4 rounded-3xl items-center justify-center transition-all duration-75"
    >
      <h2 class="text-3xl text-amber-700">Where the ISS at?</h2>
      <% if (locals.data) { %>
        <div class="lists flex flex-col items-center text-center">
  <span>Country: <%= data.country_code %></span>
         <span>Map: <a href="<%= data.map_url %>" class=" text-amber-800 hover:text-amber-700 transition-all"><%= data.map_url %></a> </span>
          <span>Time-zone: <%= data.timezone_id %></span>
        </div>
      
     <% } else { %>
<i class="text-gray-600">Nothing to display yet!</i>

      <form action="/" method="POST">
        <input
          type="submit"
          value="Locate"
          class="cursor-pointer py-2 px-3.5 bg-red-400 text-white rounded-2xl"
        />
      </form>
        <% } %>

    </div>
  </body>
</html>
